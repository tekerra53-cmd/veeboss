- [ ] Inspect existing code structure (already done: main logic in src/App.tsx).
- [ ] Add backend folder with Express server + persistent storage.
- [ ] Implement API endpoints: GET /api/content and POST /api/content (admin-protected).
- [x] Update React: replace localStorage load/save with API fetch/save.

- [x] Ensure first-run fallback returns initialContent (currently fallbackContent used until first admin save).

- [x] Update package.json scripts for running frontend + backend.
- [ ] Test: make admin change, refresh, and validate across another browser.

